<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.UserLogMapper">
    <select id="getUserLogs" resultType="com.entity.UserLog">
        SELECT log_id, user_id, type, description, time
        FROM user_logs
        WHERE user_id = #{user_id}
        ORDER BY log_id DESC
        LIMIT #{start}, #{pageSize}
    </select>
    
    <select id="getUserLogsCount" resultType="int">
        SELECT COUNT(*) FROM user_logs WHERE user_id = #{user_id}
    </select>
</mapper>
