# 数据库接口与后端接口

> **更新日期**：2026-05-31  
> **说明**：本文档已与当前部署的后端对齐。HTTP API 的完整测试说明见 **《服务器API接口说明（测试用）.md》**；后台管理 REST 规划见 **《接口设计文档.md》**（多数尚未实现）。

---

## 1. 数据库连接信息

### Neo4j（文物搜索 / 图谱数据源）

| 项目 | 值 |
|------|-----|
| 浏览器 | http://39.106.231.119:7474 |
| Bolt | bolt://39.106.231.119:7687 |
| 用户名 | neo4j |
| 密码 | password123 |

### MySQL（用户 / 收藏 / 评论）

| 项目 | 值 |
|------|-----|
| 主机 | 39.106.231.119 |
| 端口 | 3306 |
| 库名 | muse（应用配置中也可能连本机 127.0.0.1:3306/muse） |
| 账号 | 见运维配置（课设环境常用 knowledge / knowledge123） |

---

## 2. 应用服务器与 API 根地址

| 项目 | 实际配置 |
|------|----------|
| 公网 IP | `43.138.39.44`（以部署文档为准） |
| 前端 + API（推荐） | `http://43.138.39.44/`（Nginx **80** 反代） |
| Spring Boot 直连 | `http://127.0.0.1:8085`（仅服务器本机或内网） |

**请勿使用以下错误地址：**

- ~~`60.205.14.101:8080`~~ — 8080 为旧文档误写 / 本地前端开发端口，不是已部署后端
- ~~`GET /api/v1/auth/current-user`~~ — **未实现**，会 404

**正确示例：**

```bash
# 登录
curl -X POST "http://43.138.39.44/users/login" \
  -H "Content-Type: application/json" \
  -d '{"username":"1001","password":"123456"}'

# 获取用户详情（username 传数字 ID 字符串）
curl -X POST "http://43.138.39.44/users/get_detail" \
  -H "Content-Type: application/json" \
  -d '{"id":"1001"}'
```

---

## 3. 数据存储与 HTTP 接口对应关系

| 业务 | 存储 | HTTP 接口前缀 |
|------|------|----------------|
| 文物搜索 / 分类 / 详情 | Neo4j | `/search/*` |
| 知识图谱 / 看板 / 地图 / 时间轴 | Neo4j（失败时 mock） | `/api/v1/data/*` |
| 用户注册 / 登录 / 资料 | MySQL `user` 表 | `/users/*` |
| 个人中心（改资料 / 收藏 / 评论） | MySQL | `/user_admin/*` |

---

## 4. 已实现的用户相关接口（与前端对齐）

| 功能 | 方法 | 路径 | 请求体要点 |
|------|------|------|------------|
| 登录 | POST | `/users/login` | `username`（**用户 ID 字符串**）、`password` |
| 注册 | POST | `/users/register` | `username`、`password`、`sex`（`"0"`/`"1"`）、`tele` |
| 用户详情 | POST | `/users/get_detail` | `{ "id": "1001" }` |
| 修改资料 | POST | `/user_admin/update` | `id`、`oldPassword`、`name`、`sex`、`tel` |
| 我的收藏 | POST | `/user_admin/collect` | `{ "id": "1001" }` |
| 我的评论 | POST | `/user_admin/comment` | `{ "username": "1001" }` |
| 取消收藏 | POST | `/user_admin/deleteCollect` | `uid`、`rid` |
| 删除评论 | POST | `/user_admin/deleteComment` | `{ "cid": "123" }` |

**响应格式（统一）：**

```json
{ "state": 200, "message": "说明", "data": { ... }, "token": "..." }
```

成功时 `state === 200`（不是设计文档里的 `code`）。

---

## 5. 与设计文档的差异（避免联调踩坑）

| 《接口设计文档》规划路径 | 当前项目实际 |
|--------------------------|--------------|
| `POST /api/v1/auth/login` | `POST /users/login` |
| `GET /api/v1/auth/current-user` | **未实现** → 用 `POST /users/get_detail` |
| `POST /api/v1/users` 注册 | `POST /users/register` |
| `PUT /api/v1/users/{objectId}` | **未实现** → 用 `POST /user_admin/update` |
| 响应字段 `code` | 实际为 `state` |

---

## 6. 报告问题建议格式

1. 完整请求 URL（含端口）
2. HTTP 方法与 Body
3. 实际响应（状态码 + JSON）
4. 期望行为
5. 对照《服务器API接口说明（测试用）.md》章节号
